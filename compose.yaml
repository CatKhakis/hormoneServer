services:
  hormonedb:
    container_name: hormoneserver
    image: ghcr.io/catkhakis/hormoneserver:main
    ports:
      - 9100:3000
    volumes:
      - ./db:/usr/src/app/db
    restart: always
